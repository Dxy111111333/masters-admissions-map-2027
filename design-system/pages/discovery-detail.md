# Page override: discovery-detail

- Base authority: ../MASTER.md
- Discovery: wheel input belongs to the map only while the pointer is over the map; all other areas scroll the document.
- Discovery: desktop split favors the filter slightly more than the map; do not use an internally scrolling filter column.
- Detail: hero, study structure, core facts, section navigation, and full-period budget share one horizontal boundary.
- Detail: use anchor navigation and natural vertical screens rather than compressing every module into one viewport.
